<Window x:Class="PL.ChooseCallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="ChooseCallWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={ RelativeSource Mode=Self}}">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,5" >
            <TextBlock Text="MaxDistnace:" Width="120"/>
            <TextBox Text="{Binding MaxDistance, UpdateSourceTrigger=Explicit}" Width="200"
             KeyDown="TextBox_KeyDown"/>
        </StackPanel>
        <DataGrid
    ItemsSource="{Binding CallList}"
    IsReadOnly ="True" 
    AutoGenerateColumns="False"
  	    EnableRowVirtualization="True"
    RowDetailsVisibilityMode="VisibleWhenSelected"
    SelectionChanged="CallList_SelectionChanged"
    SelectedItem="{Binding SelectedCall}" Margin="0,31,0,-31">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Id" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallType}" Header="Type" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding FullAddress}" Header="Address" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding StartTime}" Header="Start Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding MaxFinishTime}" Header="Max Finish Time" Width="Auto"/>
                <DataGridTextColumn Binding="{Binding CallDistance}" Header="Distance" Width="Auto"/>
                <DataGridTemplateColumn Header="Choose" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Choose" Click="btnChooseVolunteer_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Window>
