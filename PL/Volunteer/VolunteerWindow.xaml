<!--<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VolunteerWindow" Height="700" Width="1000"
        Background="#FAFAFA"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="volunteerWindow_Loaded"
        Closed="volunteerWindow_Closed">

    --><!-- עיצוב מודרני וסגנונות --><!--
    <Window.Resources>

        --><!-- טקסטבוקס --><!--
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Color="#DDDDDD"/>
                </Setter.Value>
            </Setter>
        </Style>

        --><!-- תוויות --><!--
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        --><!-- קומבובוקס --><!--
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="0" Color="#DDDDDD"/>
                </Setter.Value>
            </Setter>
        </Style>

        --><!-- כפתורים --><!--
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="10,15"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="5" ShadowDepth="1" Color="#888"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0D47A1"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#0B3C91"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        --><!-- צ'קבוקס ורדיובוטון --><!--
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Foreground" Value="#444"/>
        </Style>

    </Window.Resources>

    --><!-- תוכן --><!--
    <ScrollViewer>
        <Border CornerRadius="10" Padding="20" Background="White" Margin="30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                --><!-- עמודה שמאל --><!--
                <Label Grid.Row="0" Grid.Column="0" Content="Id"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding CurrentVolunteer.Id, Mode=TwoWay}"
                         IsReadOnly="{Binding ButtonText, Converter={StaticResource ConvertUpdateToTrueKey}}"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Full Name"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CurrentVolunteer.FullName, Mode=TwoWay}"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Cellphone Number"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentVolunteer.CellphoneNumber, Mode=TwoWay}"/>

                <Label Grid.Row="3" Grid.Column="0" Content="Email"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentVolunteer.Email, Mode=TwoWay}"/>

                <Label Grid.Row="4" Grid.Column="0" Content="Password"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentVolunteer.Password, Mode=TwoWay}"/>

                <Label Grid.Row="5" Grid.Column="0" Content="Full Address"/>
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentVolunteer.FullAddress, Mode=TwoWay}"/>

                <Label Grid.Row="6" Grid.Column="0" Content="Role"/>
                <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Vertical">
                    <RadioButton Content="Volunteer" GroupName="Roles"
                                 IsChecked="{Binding CurrentVolunteer.Role, Converter={StaticResource RoleToBooleanConverter}, ConverterParameter=Volunteer}"/>
                    <RadioButton Content="Manager" GroupName="Roles"
                                 IsChecked="{Binding CurrentVolunteer.Role, Converter={StaticResource RoleToBooleanConverter}, ConverterParameter=Manager}"/>
                </StackPanel>

                --><!-- עמודה ימין --><!--
                <Label Grid.Row="0" Grid.Column="2" Content="Max Distance"/>
                <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding CurrentVolunteer.MaxDistance, Mode=TwoWay}"/>

                <Label Grid.Row="1" Grid.Column="2" Content="Total Handled Calls"/>
                <TextBox Grid.Row="1" Grid.Column="3" Text="{Binding CurrentVolunteer.TotalHandledCalls}" IsReadOnly="True"/>

                <Label Grid.Row="2" Grid.Column="2" Content="Total Canceled Calls"/>
                <TextBox Grid.Row="2" Grid.Column="3" Text="{Binding CurrentVolunteer.TotalCanceledCalls}" IsReadOnly="True"/>

                <Label Grid.Row="3" Grid.Column="2" Content="Total Expired Calls"/>
                <TextBox Grid.Row="3" Grid.Column="3" Text="{Binding CurrentVolunteer.TotalExpiredCalls}" IsReadOnly="True"/>

                <Label Grid.Row="4" Grid.Column="2" Content="Call In Progress"/>
                <TextBox Grid.Row="4" Grid.Column="3" Text="{Binding CurrentVolunteer.CallInProgress}" IsReadOnly="True"/>

                <Label Grid.Row="5" Grid.Column="2" Content="Is Active"/>
                <CheckBox Grid.Row="5" Grid.Column="3" IsChecked="{Binding CurrentVolunteer.IsActive}"/>

                <Label Grid.Row="6" Grid.Column="2" Content="Distance Type"/>
                <ComboBox Grid.Row="6" Grid.Column="3"
                          ItemsSource="{Binding Source={StaticResource DistanceTypeCollectionKey}}"
                          SelectedValue="{Binding CurrentVolunteer.DistanceType}"/>

                --><!-- כפתור --><!--
                <Button Grid.Row="8" Grid.ColumnSpan="4"
                        Content="{Binding ButtonText}"
                        Click="btnAddUpdate_Click"
                        Width="250" Height="45"
                        HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </ScrollViewer>
</Window>-->




<Window x:Class="PL.Volunteer.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VolunteerWindow" Height="550" Width="800"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="volunteerWindow_Loaded"
        Closed="volunteerWindow_Closed">

    <Window.Resources>
        <!-- Background gradient -->
        <LinearGradientBrush x:Key="WindowBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#f1f5f9" Offset="0"/>
            <GradientStop Color="#e2e8f0" Offset="0.5"/>
            <GradientStop Color="#cbd5e1" Offset="1"/>
        </LinearGradientBrush>

        <!-- Card background -->
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#E0FFFFFF" Opacity="0.75"/>

        <!-- Header style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#1e293b"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Label style -->
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="Margin" Value="5,3"/>
        </Style>

        <!-- TextBox style -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="2,2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1f2937"/>
            <Setter Property="BorderBrush" Value="#d1d5db"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#374151"/>
                                <Setter Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="#f8fafc"/>
                                <Setter Property="Foreground" Value="#6b7280"/>
                                <Setter Property="BorderBrush" Value="#e2e8f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox style -->
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1f2937"/>
            <Setter Property="BorderBrush" Value="#d1d5db"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <ToggleButton Name="ToggleButton" 
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Focusable="false"
                                            IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="18"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Data="M 0 0 L 3.5 3.5 L 7 0 Z" Fill="#6b7280"/>
                                    </Grid>
                                </ToggleButton>
                                <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                                Content="{TemplateBinding SelectionBoxItem}"
                                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                Margin="8,3,20,3"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"/>
                                <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Grid Name="DropDown" SnapsToDevicePixels="True"
                                          MinWidth="{TemplateBinding ActualWidth}"
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border Background="White" BorderThickness="1" BorderBrush="#d1d5db" CornerRadius="4"/>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#374151"/>
                                <Setter Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button style -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#374151"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="10,15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1f2937"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#111827"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#9ca3af"/>
                                <Setter Property="Foreground" Value="#6b7280"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox style -->
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- RadioButton style -->
        <Style TargetType="RadioButton">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="3,2"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
    </Window.Resources>

    <!-- Main background -->
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <!-- Background pattern -->
        <Canvas>
            <Rectangle Width="150" Height="150" Canvas.Left="650" Canvas.Top="80" Opacity="0.025">
                <Rectangle.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#64748b" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Rectangle Width="120" Height="120" Canvas.Left="40" Canvas.Top="400" Opacity="0.015">
                <Rectangle.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#374151" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </RadialGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
        </Canvas>

        <!-- Main content -->
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Volunteer Information" Style="{StaticResource HeaderStyle}"/>

            <!-- Content Section -->
            <Border Grid.Row="1" Background="{StaticResource CardBackgroundBrush}" 
                    CornerRadius="8" Padding="20" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect Color="#e2e8f0" BlurRadius="6" ShadowDepth="2" Opacity="0.4"/>
                </Border.Effect>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Column 1 -->
                        <Label Grid.Row="0" Grid.Column="0" Content="Id"/>
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 Text="{Binding CurrentVolunteer.Id, Mode=TwoWay}"
                                 IsReadOnly="{Binding ButtonText, Converter={StaticResource ConvertUpdateToTrueKey}}"/>

                        <Label Grid.Row="1" Grid.Column="0" Content="Full Name"/>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CurrentVolunteer.FullName, Mode=TwoWay}"/>

                        <Label Grid.Row="2" Grid.Column="0" Content="Cellphone Number"/>
                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CurrentVolunteer.CellphoneNumber, Mode=TwoWay}"/>

                        <Label Grid.Row="3" Grid.Column="0" Content="Email"/>
                        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding CurrentVolunteer.Email, Mode=TwoWay}"/>

                        <Label Grid.Row="4" Grid.Column="0" Content="Password"/>
                        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding CurrentVolunteer.Password, Mode=TwoWay}"/>

                        <Label Grid.Row="5" Grid.Column="0" Content="Full Address"/>
                        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding CurrentVolunteer.FullAddress, Mode=TwoWay}"/>

                        <Label Grid.Row="6" Grid.Column="0" Content="Role"/>
                        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                            <RadioButton Content="Volunteer" GroupName="Roles"
                                         IsChecked="{Binding CurrentVolunteer.Role, Converter={StaticResource RoleToBooleanConverter}, ConverterParameter=Volunteer}"/>
                            <RadioButton Content="Manager" GroupName="Roles"
                                         IsChecked="{Binding CurrentVolunteer.Role, Converter={StaticResource RoleToBooleanConverter}, ConverterParameter=Manager}"/>
                        </StackPanel>

                        <!-- Column 2 -->
                        <Label Grid.Row="0" Grid.Column="3" Content="Max Distance"/>
                        <TextBox Grid.Row="0" Grid.Column="4" Text="{Binding CurrentVolunteer.MaxDistance, Mode=TwoWay}"/>

                        <Label Grid.Row="1" Grid.Column="3" Content="Total Handled Calls"/>
                        <TextBox Grid.Row="1" Grid.Column="4" Text="{Binding CurrentVolunteer.TotalHandledCalls}" IsReadOnly="True"/>

                        <Label Grid.Row="2" Grid.Column="3" Content="Total Canceled Calls"/>
                        <TextBox Grid.Row="2" Grid.Column="4" Text="{Binding CurrentVolunteer.TotalCanceledCalls}" IsReadOnly="True"/>

                        <Label Grid.Row="3" Grid.Column="3" Content="Total Expired Calls"/>
                        <TextBox Grid.Row="3" Grid.Column="4" Text="{Binding CurrentVolunteer.TotalExpiredCalls}" IsReadOnly="True"/>

                        <Label Grid.Row="4" Grid.Column="3" Content="Call In Progress"/>
                        <TextBox Grid.Row="4" Grid.Column="4" Text="{Binding CurrentVolunteer.CallInProgress}" IsReadOnly="True"/>

                        <Label Grid.Row="5" Grid.Column="3" Content="Is Active"/>
                        <CheckBox Grid.Row="5" Grid.Column="4" IsChecked="{Binding CurrentVolunteer.IsActive}" VerticalAlignment="Center"/>

                        <Label Grid.Row="6" Grid.Column="3" Content="Distance Type"/>
                        <ComboBox Grid.Row="6" Grid.Column="4"
                                  ItemsSource="{Binding Source={StaticResource DistanceTypeCollectionKey}}"
                                  SelectedValue="{Binding CurrentVolunteer.DistanceType}"/>
                    </Grid>
                </ScrollViewer>
            </Border>

            <!-- Action Button -->
            <Button Grid.Row="2"
                    Content="{Binding ButtonText}"
                    Click="btnAddUpdate_Click"
                    Width="200" Height="40"
                    HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</Window>